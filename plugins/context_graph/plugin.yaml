name: context-graph
version: 1.0.0
description: "Records agent work (files, commands, searches, memories) as a graph of nodes and edges, and recalls ranked subgraphs per turn (waterfall loader). Exposes the ariadne_graph tool."
author: "Ariadne"
hooks:
  - post_tool_call
  - pre_llm_call
  - on_session_end
